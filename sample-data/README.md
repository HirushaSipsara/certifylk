# Sample data

`chilli-paste-profile.json` and `chilli-paste-process.json` document the deterministic legacy sample used by `POST /api/v1/assessments/sample`. The endpoint exercises the existing global requirement/scoring/roadmap engine and preserves the `32.0000 / 32` regression baseline.

This sample is not tied to a verified product standard and must not be presented as an SLS certificate-specific assessment. The target replacement is a clearly labelled read-only Fresh Fruit Cordial / SLS Mark sample built through the frozen scheme-specific workflow described in `../docs/FULL_IMPLEMENTATION_PLAN.md`.

All sample evidence must remain synthetic/non-sensitive. No sample result is certification, legal advice, or an official inspection conclusion.
